{
  "buildCommand": "npm run build && npx playwright install chromium",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "app/actions/crawl.ts": {
      "maxDuration": 60
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
